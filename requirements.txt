autoenv                     # runs commands when cd-ing into a folder
beautifulsoup4              # for scraping and parsing web documents
flask                       # web micro-framework
flask-sqlalchemy            # SQLAlchemy for Flask
flask-migrate               # Flask SQLAlchemy database migrations using Alembic
gunicorn                    # web server gateway interface (WSGI)
mypy                        # python static type checker
nltk                        # Natural language processing toolkit
psycopg2                    # a python adapter for Postgres
redis                       # python interface to Redis
requests                    # for sending http requests
rq                          # Redis Queue; library for queuing jobs

# Dev Tools; Not needed for distribution
pytest                      # code tester
yapf                        # code formatter
sphinx                      # documentation generator
sphinx_rtd_theme            # sphinx ReadTheDocs theme
tox                         # tool for checking python packages in different versions of python